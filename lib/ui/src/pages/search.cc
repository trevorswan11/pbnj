#include "ui/pages/search.hh"

#include <stdx/utility.hh>

#include "ui/core/component.hh"

namespace pbnj::ui::pages {

auto search::on_mount(context& ctx) -> void { DISCARD(ctx); }

auto search::on_unmount(context& ctx) -> void { DISCARD(ctx); }

auto search::render(context& ctx) -> void { DISCARD(ctx); }

} // namespace pbnj::ui::pages
