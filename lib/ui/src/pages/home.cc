#include "ui/pages/home.hh"

#include <stdx/utility.hh>

#include "ui/core/component.hh"

namespace pbnj::ui::pages {

auto home::on_mount(context& ctx) -> void { DISCARD(ctx); }

auto home::on_unmount(context& ctx) -> void { DISCARD(ctx); }

auto home::render(context& ctx) -> void { DISCARD(ctx); }

} // namespace pbnj::ui::pages
