#include "ui/pages/player.hh"

#include <stdx/utility.hh>

#include "ui/core/component.hh"

namespace pbnj::ui::pages {

auto player::on_mount(context& ctx) -> void { DISCARD(ctx); }

auto player::on_unmount(context& ctx) -> void { DISCARD(ctx); }

auto player::render(context& ctx) -> void { DISCARD(ctx); }

} // namespace pbnj::ui::pages
