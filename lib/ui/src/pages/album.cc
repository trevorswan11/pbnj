#include "ui/pages/album.hh"

#include <stdx/utility.hh>

#include "ui/core/component.hh"

namespace pbnj::ui::pages {

auto album::on_mount(context& ctx) -> void { DISCARD(ctx); }

auto album::on_unmount(context& ctx) -> void { DISCARD(ctx); }

auto album::render(context& ctx) -> void { DISCARD(ctx); }

} // namespace pbnj::ui::pages
